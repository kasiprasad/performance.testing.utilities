add an id to the Report model 

use a central repository to store the Report model

update index.html to support querystring params for left and right reports

update index.html to better support viewing two reports (jquery, allow hiding of frames

update index.html to support search of test results

upon completion of a perf run, publish the index.html file with a static left and right report built in